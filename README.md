# VeloxQuant Backend (FastAPI)

- Démarrage local :
  - python -m venv .venv && .venv\Scripts\activate
  - pip install -r requirements.txt
  - uvicorn server:app --reload --port 8001

# VeloxQuant Frontend (React)

- Démarrage local :
  - yarn install
  - yarn start
